#include <stdio.h>

int main(){
    
    int a = 14, b = 13;
    int add, mod, sub, div, multi, average;

    add = a + b;
    sub = a - b;
    div = a/b;
    multi = a*b;
    average = add/2;
 
    // printf("addition of the given two number is: %d\n", add);


    printf("Sum of a and b : %d\n", add);
    printf("Substraction of a and b : %d\n", sub);
    printf("Division: %d, Multiplication: %d, Average: %d ", div, multi, average);


    /* This is the comment of C programming */
    
    

    return 0;
}