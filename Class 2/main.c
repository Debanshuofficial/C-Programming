#include<stdio.h> // this is called header file.

int main()
{
    int a, b;
    float c;
    printf("Enter 1st number:");
    // a = input("enter value");  || This step is for python.
    scanf("%d", &a);
    printf("Your number is %d and age is %d", a,b);
    //%d is a format specifier for integer value.
    return 0;
}


// each line contains tokens.
// keywords, identifiers, constants, symbols, string laterals.
// Total 32 keywords are in C languages.